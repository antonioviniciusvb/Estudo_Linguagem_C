#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)

    {
    int contador = 1;// inicializa a variavel de controle

    while(contador<=100) // testa a variavel de controle
    { // inicio do loop

        printf("%d",contador); // executa os comandos
        contador++; // incrementa a variavel de controle

    }// fim do loop

    getch();
    return(0);



}
